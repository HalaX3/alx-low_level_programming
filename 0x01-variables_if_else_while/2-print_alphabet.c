#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C program that prints the last digit of a number
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a, z = 'z';

	for (a = 'a'; a <= z; a++)
	{
		putchar(a);
	}
	return (0);
}
